# 油猴脚本开发项目 - Cursor 规则

## 项目概述
这是一个基于 webpack + Vue.js 的油猴脚本开发脚手架项目，专门用于：
- 🕷️ **爬虫 JS 逆向开发**
- 🐒 **油猴脚本实时调试**
- 🔧 **快速原型开发和测试**
- 📦 **最终打包成独立的 JS 油猴脚本**

## 技术栈
- **构建工具**: Webpack 5 + 热重载
- **前端框架**: Vue.js 2.7
- **样式处理**: Less + CSS Loader
- **调试环境**: Chrome DevTools + VS Code 调试
- **目标输出**: 单文件 JS 油猴脚本

## 开发流程
1. **开发阶段**: 在 `src/` 目录下编写 Vue 组件和逻辑
2. **调试阶段**: 使用 `npm start` 启动开发服务器，实时调试
3. **测试阶段**: 通过油猴脚本在目标网站上测试功能
4. **打包阶段**: 使用 `npm run build` 生成最终油猴脚本

## 文件结构说明
```
src/
├── index.js          # 应用入口，创建 DOM 容器并挂载 Vue 应用
├── app.vue           # 主 Vue 组件，包含油猴脚本的 UI 界面
└── app.less          # 样式文件

tampermonkey.js       # 油猴脚本模板，用于动态加载开发中的代码
webpack.config.*.js   # Webpack 配置文件，支持开发和生产环境
```

## 调试配置
- **开发服务器**: `http://localhost:8080`
- **Chrome 调试端口**: 9222
- **源码映射**: 支持断点调试和热重载
- **VS Code 集成**: 完整的调试配置和源码映射

## 使用场景
- **网站数据抓取**: 开发爬虫脚本，实时提取网页数据
- **页面功能增强**: 为现有网站添加新功能
- **自动化操作**: 实现页面操作的自动化脚本
- **JS 逆向研究**: 分析和调试网站的前端逻辑

## 开发注意事项
1. **油猴脚本限制**: 注意跨域限制和网站安全策略
2. **DOM 操作**: 确保脚本在目标页面 DOM 加载完成后执行
3. **错误处理**: 添加适当的错误处理，避免脚本崩溃
4. **性能优化**: 避免阻塞页面渲染，使用异步操作

## 调试技巧
- 在 `src/` 目录下的源码中设置断点
- 使用 `console.log()` 输出调试信息
- 通过 Chrome DevTools 查看网络请求和控制台输出
- 利用 Vue DevTools 调试组件状态

## 最终输出
项目最终会生成一个独立的 JS 文件，可以直接复制到油猴扩展中使用，实现：
- 实时数据抓取
- 页面功能增强
- 自动化操作
- 逆向分析工具

这是一个专业的油猴脚本开发环境，特别适合需要快速迭代和实时调试的爬虫和逆向项目。
